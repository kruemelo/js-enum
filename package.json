{
  "name": "enum",
  "version": "1.0.0",
  "description": "javascript enum",
  "main": "enum.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "javascript",
    "enum"
  ],
  "author": "kruemelo",
  "license": "MIT",
  "devDependencies": {
    "mocha": "^2.1.0",
    "requirejs": "^2.1.15"
  }
}
